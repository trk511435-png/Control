const brokerUrl = "wss://7f9dc9fedda84268a977070b2dfd69af.s1.eu.hivemq.cloud:8884/mqtt";
const options = {
  clientId: "sm_store_web_" + Math.random().toString(16).substring(2, 8),
  username: "S",
  password: "sss435738", // أو كلمة المرور الجديدة لو غيرتها في Access Management
  clean: true,
  connectTimeout: 4000
};

const mqttClient = mqtt.connect(brokerUrl, options);

mqttClient.on("connect", () => {
  console.log("تم الاتصال بنجاح بسيرفر HiveMQ السحابي الجديد لـ SM Store!");
});
